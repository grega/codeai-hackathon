"""Mock providers.

These make the whole experience clickable straight after a git clone, before any
model, API key or asset exists. Each one is small and heavily commented — copy
the file next to it into providers/real/ and replace the body.
"""
